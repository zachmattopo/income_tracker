import 'package:flutter/material.dart';

class TabWeeklyEarns extends StatelessWidget {
  const TabWeeklyEarns({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Weekly Earnings Tab'),
    );
  }
}
